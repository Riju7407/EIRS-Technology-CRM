services:
  - type: web
    name: eirs-crm-backend
    root: .
    runtime: node
    buildCommand: cd server && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
