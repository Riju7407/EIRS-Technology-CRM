services:
  - type: web
    name: eirs-crm-backend
    root: .
    runtime: node
    buildCommand: npm install --prefix server
    startCommand: npm --prefix server start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
